# `03-rest-holidays`

| Folder | Role |
|--------|------|
| **`client/`** | React + TypeScript (Vite) app |
| **`server/`** | Fastify REST API (fixtures in `server/data/*.json`) |

1. **`cd server && npm install && npm run dev`** (port **4010** by default).  
2. **`cd client && npm install && npm run dev`**.

Task spec and scoring: root **`README.md`** in the repo.
