# `03-rest-holidays`

| Folder | Role |
|--------|------|
| **`client/`** | React + TypeScript (Vite) — your work |
| **`server/`** | Fastify API (fixtures in `server/data/*.json`) |

1. **`cd server && npm install && npm run dev`**.  
2. **`cd client && npm install && npm run dev`**.

Scoring: repo root **`README.md`**.
