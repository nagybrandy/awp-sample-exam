# Dev: omit if you use Vite’s /api → `server/` proxy.
# If the Fastify server uses another port, set when starting Vite (not in .env by default):
# HOLIDAYS_API_PROXY_TARGET=http://127.0.0.1:4011
#
# Build / preview: set absolute API base, e.g.:
# VITE_HOLIDAYS_API_URL=http://127.0.0.1:4010
